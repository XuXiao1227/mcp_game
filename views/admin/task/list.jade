.container-fluid(style="padding-left:0px")
    button.btn.btn-default(id=id + '_search', type="button", data-loading-text="处理中..", autocomplete="off") 搜索
.container-fluid(style="padding-left:0px;padding-top:6px;")
    table.table.table-striped.table-bordered.table-hover
        thead
            tr
                td 名称
                td 开始时间
                td 结束时间
                td 奖励经验值
                td 奖励金币
                td 类型
                td 当前状态
        tbody
            - each val in rst
                tr
                    td=val.name
                    td=val.start_time
                    td=val.end_time
                    td=val.reward_exp
                    td=val.reward_gold
                    td=val.type.des
                    td=val.status.des
.container-fluid(id=id + '_pageBar', style="padding-left:0px;")
script(type='text/javascript', src='./js/' + jadePath + '.js')
script(type='text/javascript')
    |CurSite.IMPL['#{id}'] = new CurSite.COMP.AdminTaskList({id:'#{id}', parent:'#{parent}', skip:'#{skip}', limit:'#{limit}', count:'#{count}'});